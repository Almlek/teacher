# ملاحظات معاينة الوحدات الجديدة

تمت معاينة الصفحة الرئيسية وصفحات `/exams` و`/exams/editor` و`/backup` على سطح المكتب بمقاس 1280×900. ظهر شريط التنقل العربي RTL متضمناً الرئيسية، تحضير جديد، المكتبة، الدروس المحفوظة، الاختبارات، الأرشيف، الإعدادات، والنسخ الاحتياطي.

ظهرت صفحات الاختبارات ومحرر الاختبارات والنسخ الاحتياطي بتصميم موحد، وبطاقات واضحة للوظائف المستهدفة، مع روابط انتقال مباشرة إلى محرر الاختبارات أو الإعدادات. هذه الصفحات تمثل طبقة التنقل والواجهة الأولية، بينما ستتصل إجراءاتها بالبيانات في مراحل الاختبارات والأرشيف والنسخ الاحتياطي اللاحقة.
