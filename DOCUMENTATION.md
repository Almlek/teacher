# دفتر التحضير الذكي - دليل المستخدم والمطور

## نظرة عامة

**دفتر التحضير الذكي** هو تطبيق ويب متكامل يساعد المعلمين على إنشاء خطط دروس احترافية باستخدام الذكاء الاصطناعي (Gemini). يوفر التطبيق واجهة عربية RTL أنيقة مع ميزات متقدمة للتوليد والتصدير والإدارة.

## الميزات الرئيسية

### 1. إنشاء خطط الدروس الذكية
- **نموذج شامل**: إدخال معلومات الحصة (مدرسة، معلم، مادة، صف، شعبة، تاريخ، حصة)
- **توليد ذكي**: استخدام Gemini AI لتوليد محتوى الدرس تلقائياً
- **خيارات متقدمة**:
  - اختيار اللغة (عربي/إنجليزي)
  - اختيار نموذج AI (Flash للسرعة، Pro للجودة العالية)
  - **مصدر المحتوى**: عنوان فقط، نص، صور، PDF، Word DOCX، أو مرجع من المكتبة

### 2. محتوى متعدد الأشكال
كل خطة درس تتضمن:
- **محتوى الدرس**: الشرح الكامل والمفاهيم الأساسية
- **السبورة الذكية**: ملخص مرئي مختصر للدرس
- **الملخص التفاعلي**: أسئلة وأجوبة للتقييم الذاتي
- **الخريطة الذهنية**: تمثيل منظم للمفاهيم والعلاقات
- **حل التقويم**: إجابات نموذجية قابلة للمراجعة

### 3. الأرشيف المنظم
- حفظ جميع الخطط في قاعدة بيانات آمنة
- البحث والتصفية حسب المادة والصف والتاريخ
- إعادة استخدام الخطط السابقة

### 4. المكتبة الرقمية
- رفع الكتب والمراجع (PDF وWord DOCX)
- استخراج النص من PDF وDOCX وبناء فهرس مبسط من العناوين
- قارئ ومعاينة للملف مع زر استخدام النص في تحضير درس
- تنظيم المكتبة حسب المادة والصف
- الوصول السريع للمراجع أثناء التحضير

### 5. التصدير والمشاركة
- **تصدير PDF**: يفتح نسخة طباعة عربية RTL تتضمن معلومات الخطة وجميع أقسامها (محتوى الدرس، السبورة الذكية، والملخص التفاعلي). من نافذة الطباعة اختر **Save as PDF / حفظ بصيغة PDF**، وسيستخدم المتصفح عنوان الخطة اسماً افتراضياً للملف.
- **تصدير HTML**: ملف منسق جاهز للطباعة والعرض
- **تصدير نصي**: ملف نص عادي للتحرير
- **نسخ مباشر**: نسخ المحتوى إلى الحافظة

### 6. الإعدادات الشخصية
- **الثيمات اللونية**: بنفسجي، كحلي، أزرق، أخضر
- **حجم الخط**: صغير، متوسط، كبير
- **نوع الخط**: دعم الخطوط العربية المتعددة
- **الإعدادات الافتراضية**: حفظ البيانات الافتراضية للسرعة

## دليل الاستخدام

### البدء السريع

1. **تسجيل الدخول**
   - انقر على "تسجيل الدخول" في الصفحة الرئيسية
   - استخدم حسابك عبر Manus OAuth

2. **إنشاء خطة جديدة**
   - انقر على "تحضير جديد" من الصفحة الرئيسية
   - ملء معلومات الحصة
   - أدخل عنوان الدرس
   - اختر نموذج AI المفضل
   - انقر على "توليد وحفظ الخطة"

3. **عرض الخطة**
   - ستنتقل تلقائياً لصفحة عرض الخطة بعد التوليد
   - استعرض المحتوى في التبويبات الثلاث
   - استخدم أزرار التصدير حسب احتياجك
   - عند اختيار **PDF** ستظهر نافذة الطباعة، وتظهر الأقسام الثلاثة معاً في النسخة المطبوعة حتى لو كان قسم واحد فقط ظاهراً أثناء التصفح
   - اختر حجم الورق A4 عند الحاجة، ثم اختر **حفظ بصيغة PDF** من وجهة الطباعة المناسبة

### الأرشيف

- انقر على "الأرشيف" لعرض جميع الخطط السابقة
- استخدم البحث للعثور على خطة محددة
- انقر على أي خطة لعرضها أو تحريرها

### المكتبة

- انقر على "المكتبة" لإدارة الملفات
- استخدم "إضافة كتاب" لرفع ملفات PDF جديدة
- نظم الملفات حسب المادة والصف

### الإعدادات

- انقر على "الإعدادات" لتخصيص التطبيق
- غير الثيمة والخطوط والإعدادات الافتراضية
- حفظ التغييرات تلقائياً

## البنية التقنية

### المكدس التقني

**الأمامي (Frontend):**
- React 19 مع TypeScript
- Tailwind CSS 4 للتصميم
- Wouter للتوجيه
- tRPC للاتصال بالخادم
- Streamdown لعرض Markdown

**الخلفي (Backend):**
- Express.js للخادم
- tRPC للـ API
- Drizzle ORM لقاعدة البيانات
- MySQL/TiDB لتخزين البيانات
- Gemini AI API للتوليد الذكي

**المصادقة:**
- Manus OAuth للتسجيل الآمن
- JWT للجلسات

### قاعدة البيانات

**الجداول الرئيسية:**

1. **users**: بيانات المستخدمين
2. **lesson_plans**: خطط الدروس مع المحتوى المولد
3. **library_books**: الملفات المرفوعة والنصوص المستخرجة
4. **exams**: الاختبارات المحفوظة وأنواعها ومحتواها
5. **exam_questions**: الأسئلة والخيارات والإجابات والدرجات
6. **user_settings**: تفضيلات المستخدم وإعدادات محركات التوليد

## دليل المطور

### الإعداد المحلي

```bash
# تثبيت الاعتماديات
pnpm install

# تشغيل خادم التطوير
pnpm dev

# تشغيل الاختبارات
pnpm test

# فحص الأنواع
pnpm check
```

### هيكل المشروع

```
smart_lesson_planner/
├── client/                    # الواجهة الأمامية
│   ├── src/
│   │   ├── pages/            # الصفحات الرئيسية
│   │   ├── components/       # المكونات المعاد استخدامها
│   │   ├── lib/              # الدوال المساعدة
│   │   └── contexts/         # React Contexts
│   └── index.html
├── server/                    # الخادم والـ API
│   ├── routers.ts            # تعريفات tRPC
│   ├── db.ts                 # استعلامات قاعدة البيانات
│   ├── lessonGenerator.ts    # توليد الخطط بـ AI
│   └── _core/                # الملفات الأساسية
├── drizzle/                   # تعريفات قاعدة البيانات
│   └── schema.ts
└── shared/                    # الأنواع المشتركة
```

### إضافة ميزة جديدة

1. **تحديث قاعدة البيانات**
   ```bash
   # عدّل drizzle/schema.ts
   # ثم شغّل:
   pnpm drizzle-kit generate
   # وطبّق الهجرة عبر webdev_execute_sql
   ```

2. **إضافة استعلام قاعدة بيانات**
   - أضف الدالة في `server/db.ts`

3. **إضافة إجراء tRPC**
   - أضف الإجراء في `server/routers.ts`

4. **بناء الواجهة الأمامية**
   - أنشئ صفحة جديدة في `client/src/pages/`
   - استخدم tRPC hooks للاتصال بالخادم

5. **كتابة الاختبارات**
   - أضف اختبارات في `server/*.test.ts`
   - شغّل `pnpm test` للتحقق

### الاختبارات

اختبارات التصدير موجودة في `client/src/lib/printExport.test.ts`، وتشمل تنظيف اسم الخطة، العنوان الاحتياطي عند غياب الاسم، وتشغيل الطباعة ثم استعادة عنوان المستند بعد اكتمالها.

```bash
# تشغيل جميع الاختبارات
pnpm test

# تشغيل اختبار محدد
pnpm test lessons.test.ts

# الاختبارات المتاحة:
# - auth.logout.test.ts: اختبارات المصادقة
# - lessons.test.ts: اختبارات إدارة الخطط والتوليد
```

## الأداء والتحسينات

### التحسينات المنفذة

1. **تحسين الاستعلامات**
   - استخدام Drizzle ORM للاستعلامات الفعالة
   - فهرسة الحقول المهمة في قاعدة البيانات

2. **تحسين الواجهة**
   - استخدام React Query للتخزين المؤقت
   - تحميل كسول للمكونات الثقيلة
   - تحسين الرسوم المتحركة والانتقالات

3. **تحسين التوليد**
   - استخدام Gemini Flash للسرعة
   - معالجة غير متزامنة للعمليات الطويلة

## استكشاف الأخطاء

### مشاكل شائعة

**المشكلة**: التطبيق بطيء
- **الحل**: تحقق من سرعة الإنترنت، استخدم Gemini Flash بدلاً من Pro

**المشكلة**: فشل التوليد
- **الحل**: تحقق من صحة مفتاح Gemini API، تأكد من الاتصال بالإنترنت

**المشكلة**: لا تظهر الخطط المحفوظة
- **الحل**: تحقق من تسجيل الدخول، امسح ذاكرة التخزين المؤقت

## الدعم والمساهمة

للإبلاغ عن مشاكل أو اقتراح ميزات جديدة، يرجى التواصل عبر:
- البريد الإلكتروني: support@smart-lesson-planner.com
- الموقع: https://smart-lesson-planner.com

## الترخيص

هذا المشروع مرخص تحت MIT License.

---

**آخر تحديث**: أغسطس 2026
**الإصدار**: 1.1.0

## رفع ملفات PDF والصور إلى المكتبة

تدعم المكتبة الرقمية الآن رفع ملفات PDF وصور JPG وPNG وWEBP وGIF من خلال حوار رفع مباشر داخل صفحة المكتبة. يقرأ المتصفح الملف محلياً ثم يرسله إلى الخادم، حيث يتم التحقق من نوعه وتوقيعه وحجمه قبل تخزينه في التخزين السحابي وربط رابطه وسجله بحساب المستخدم.

الحد الأقصى للملف هو 10 ميجابايت. تحفظ قاعدة البيانات اسم الملف، نوعه، حجمه، مفتاح التخزين، ورابط الوصول، بينما تبقى بايتات الملف في التخزين السحابي ولا تُحفظ داخل قاعدة البيانات. تعرض المكتبة معاينة مباشرة للصور وملفات PDF، مع إمكانية فتح الملف أو تنزيله وحذفه من السجل.

### المعاينة العملية لمسار الرفع

تتميز واجهة رفع الملفات في المكتبة الرقمية بما يلي:
1. **الكشف التلقائي لنوع الملف**: يتم التحقق فوريّاً من الملف المختار وعرض نوعه بدقة (مثل PDF أو صورة PNG/JPG).
2. **المعاينة الفورية قبل الإرسال**: تظهر معاينة مرئية مباشرة للملف المختار (صورة مصغرة أو عارض PDF) داخل حوار الرفع قبل الضغط على زر الاعتماد.
3. **التحقق من الحدود**: يرفض النظام الملفات التي تتجاوز 10 ميجابايت أو غير المطابقة للتوقيع الرقمي المعتمد.


## توسعة منصة ailessonexams

تضم الواجهة الآن تنقلاً موحداً بين **تحضير جديد** و**المكتبة** و**الدروس المحفوظة** و**الاختبارات** و**الأرشيف** و**الإعدادات** و**النسخ الاحتياطي**. تعرض صفحة الأرشيف الموحد الخطط والسبورات والملخصات والخرائط وإجابات التقويم والاختبارات ضمن تبويبات مستقلة.

### الاختبارات ومحرر الأسئلة

تتيح صفحة الاختبارات إنشاء اختبار شامل أو رسمي أو إلكتروني. يدعم المحرر أسئلة الاختيار من متعدد، والصح والخطأ، والإجابة القصيرة، والأسئلة المقالية، مع الخيارات والإجابة النموذجية والشرح والدرجة. تحفظ الاختبارات في جدولي `exams` و`exam_questions`، ويمكن تعديلها من خلال رابط المحرر داخل الأرشيف.

### قراءة PDF وWord

تقبل المكتبة ملفات PDF وصيغة Word الحديثة **DOCX** فقط؛ حُصر الدعم في DOCX لأن استخراج ملفات DOC القديمة يحتاج محركاً مختلفاً. يتحقق الخادم من توقيع الملف وحجمه قبل التخزين السحابي، ثم يحاول استخراج النص وبناء فهرس مبسط من العناوين. يمكن معاينة النص المستخرج واستخدامه مباشرة في نموذج تحضير درس جديد. أما الصور فتظل قابلة للمعاينة والتنزيل دون استخراج نص تلقائي.

### الإعدادات والنسخ الاحتياطي

تتضمن الإعدادات بيانات المدرسة والمعلم والمديرية والمادة واللغة ونموذج Gemini ونوع الاختبار الافتراضي وأنواع الأسئلة ومحركات التوليد. تُستخدم هذه القيم فعلياً كقيم ابتدائية في نموذج التحضير ومحرر الاختبارات. تنشئ صفحة النسخ الاحتياطي ملف JSON يضم الخطط والمراجع والاختبارات وأسئلة الاختبارات والإعدادات، وتعيد ربط الأسئلة بالاختبارات عند الاستيراد. عملية الحذف الكلي حساسة وتتطلب تأكيداً صريحاً.

### التحقق والجودة

تشمل الاختبارات الجديدة تحقق توقيعات DOCX، واستعادة الاختبارات مع أسئلتها، وتطبيق إعدادات `defaultExamType` و`generationTargets` داخل الصفحات، إلى جانب اختبارات التوليد والتصدير ورفع الملفات الموجودة سابقاً.

### التوليد الذكي للاختبارات من محتوى الدرس

تتيح ميزة **توليد اختبار ذكي** اختيار أي خطة درس محفوظة مسبقاً، وتحديد نوع الاختبار ومستوى الصعوبة وعدد الأسئلة والنموذج. يدمج الخادم محتوى الدرس وأقسامه المختلفة، ويستولد أسئلة دقيقة ذات خيارات وإجابات نموذجية باستخدام `invokeLLM` عبر المخطط الهيكلي (JSON Schema)، ثم يحفظ الاختبار وأسئلته في قاعدة البيانات ويربطها بالدرس عبر `sourceLessonId`.

### تصدير الاختبارات إلى PDF وWord

يحتوي محرر الاختبارات على زرين لتصدير ورقة الاختبار. يفتح **تصدير PDF** معاينة طباعة عربية باتجاه RTL وحجم A4، تتضمن عنوان الاختبار وبيانات المادة والصف والمدة والدرجة وخطوط اسم الطالب والتاريخ والأسئلة والخيارات. بعد ذلك يختار المستخدم من نافذة المتصفح خيار **حفظ بصيغة PDF**.

ينشئ **تصدير Word** ملفاً بصيغة `.doc` متوافقة مع Microsoft Word، وتحتوي داخله على HTML منسق باتجاه RTL مع ترميز UTF-8، وهو مناسب للطباعة والتحرير والمشاركة. يتضمن الملف نفس بيانات الاختبار والأسئلة والخيارات، ويستخدم اسماً عربياً آمناً بعد إزالة المحارف غير الصالحة لأسماء الملفات.

### تحرير وإعادة ترتيب الأسئلة

يتيح محرر الاختبارات للمستخدم إمكانية تعديل أي سؤال مولّد بالذكاء الاصطناعي يدوياً (تعديل النص، الدرجة، الخيارات، الإجابة الصحيحة والشرح)، بالإضافة إلى إعادة ترتيب الأسئلة باستخدام أزرار النقل (للأعلى وللأسفل) أو حذف الأسئلة غير المرغوبة. يتم حفظ الترتيب الجديد وتحديث الفهارس بشكل متسلسل، وينعكس مباشرة على التصدير إلى PDF وWord وحفظ قاعدة البيانات.
