# دفتر التحضير الذكي - دليل المستخدم والمطور

## نظرة عامة

**دفتر التحضير الذكي** هو تطبيق ويب متكامل يساعد المعلمين على إنشاء خطط دروس احترافية باستخدام الذكاء الاصطناعي (Gemini). يوفر التطبيق واجهة عربية RTL أنيقة مع ميزات متقدمة للتوليد والتصدير والإدارة.

## الميزات الرئيسية

### 1. إنشاء خطط الدروس الذكية
- **نموذج شامل**: إدخال معلومات الحصة (مدرسة، معلم، مادة، صف، شعبة، تاريخ، حصة)
- **توليد ذكي**: استخدام Gemini AI لتوليد محتوى الدرس تلقائياً
- **خيارات متقدمة**:
  - اختيار اللغة (عربي/إنجليزي)
  - اختيار نموذج AI (Flash للسرعة، Pro للجودة العالية)
  - مصدر المحتوى (عنوان فقط، نص، صور، PDF)

### 2. محتوى متعدد الأشكال
كل خطة درس تتضمن:
- **محتوى الدرس**: الشرح الكامل والمفاهيم الأساسية
- **السبورة الذكية**: ملخص مرئي مختصر للدرس
- **الملخص التفاعلي**: أسئلة وأجوبة للتقييم الذاتي

### 3. الأرشيف المنظم
- حفظ جميع الخطط في قاعدة بيانات آمنة
- البحث والتصفية حسب المادة والصف والتاريخ
- إعادة استخدام الخطط السابقة

### 4. المكتبة الرقمية
- رفع الكتب والمراجع (PDF)
- تنظيم المكتبة حسب المادة والصف
- الوصول السريع للمراجع أثناء التحضير

### 5. التصدير والمشاركة
- **تصدير HTML**: ملف منسق جاهز للطباعة والعرض
- **تصدير نصي**: ملف نص عادي للتحرير
- **نسخ مباشر**: نسخ المحتوى إلى الحافظة

### 6. الإعدادات الشخصية
- **الثيمات اللونية**: بنفسجي، كحلي، أزرق، أخضر
- **حجم الخط**: صغير، متوسط، كبير
- **نوع الخط**: دعم الخطوط العربية المتعددة
- **الإعدادات الافتراضية**: حفظ البيانات الافتراضية للسرعة

## دليل الاستخدام

### البدء السريع

1. **تسجيل الدخول**
   - انقر على "تسجيل الدخول" في الصفحة الرئيسية
   - استخدم حسابك عبر Manus OAuth

2. **إنشاء خطة جديدة**
   - انقر على "تحضير جديد" من الصفحة الرئيسية
   - ملء معلومات الحصة
   - أدخل عنوان الدرس
   - اختر نموذج AI المفضل
   - انقر على "توليد وحفظ الخطة"

3. **عرض الخطة**
   - ستنتقل تلقائياً لصفحة عرض الخطة بعد التوليد
   - استعرض المحتوى في التبويبات الثلاث
   - استخدم أزرار التصدير حسب احتياجك

### الأرشيف

- انقر على "الأرشيف" لعرض جميع الخطط السابقة
- استخدم البحث للعثور على خطة محددة
- انقر على أي خطة لعرضها أو تحريرها

### المكتبة

- انقر على "المكتبة" لإدارة الملفات
- استخدم "إضافة كتاب" لرفع ملفات PDF جديدة
- نظم الملفات حسب المادة والصف

### الإعدادات

- انقر على "الإعدادات" لتخصيص التطبيق
- غير الثيمة والخطوط والإعدادات الافتراضية
- حفظ التغييرات تلقائياً

## البنية التقنية

### المكدس التقني

**الأمامي (Frontend):**
- React 19 مع TypeScript
- Tailwind CSS 4 للتصميم
- Wouter للتوجيه
- tRPC للاتصال بالخادم
- Streamdown لعرض Markdown

**الخلفي (Backend):**
- Express.js للخادم
- tRPC للـ API
- Drizzle ORM لقاعدة البيانات
- MySQL/TiDB لتخزين البيانات
- Gemini AI API للتوليد الذكي

**المصادقة:**
- Manus OAuth للتسجيل الآمن
- JWT للجلسات

### قاعدة البيانات

**الجداول الرئيسية:**

1. **users**: بيانات المستخدمين
2. **lesson_plans**: خطط الدروس مع المحتوى المولد
3. **library_books**: الملفات المرفوعة
4. **user_settings**: تفضيلات المستخدم

## دليل المطور

### الإعداد المحلي

```bash
# تثبيت الاعتماديات
pnpm install

# تشغيل خادم التطوير
pnpm dev

# تشغيل الاختبارات
pnpm test

# فحص الأنواع
pnpm check
```

### هيكل المشروع

```
smart_lesson_planner/
├── client/                    # الواجهة الأمامية
│   ├── src/
│   │   ├── pages/            # الصفحات الرئيسية
│   │   ├── components/       # المكونات المعاد استخدامها
│   │   ├── lib/              # الدوال المساعدة
│   │   └── contexts/         # React Contexts
│   └── index.html
├── server/                    # الخادم والـ API
│   ├── routers.ts            # تعريفات tRPC
│   ├── db.ts                 # استعلامات قاعدة البيانات
│   ├── lessonGenerator.ts    # توليد الخطط بـ AI
│   └── _core/                # الملفات الأساسية
├── drizzle/                   # تعريفات قاعدة البيانات
│   └── schema.ts
└── shared/                    # الأنواع المشتركة
```

### إضافة ميزة جديدة

1. **تحديث قاعدة البيانات**
   ```bash
   # عدّل drizzle/schema.ts
   # ثم شغّل:
   pnpm drizzle-kit generate
   # وطبّق الهجرة عبر webdev_execute_sql
   ```

2. **إضافة استعلام قاعدة بيانات**
   - أضف الدالة في `server/db.ts`

3. **إضافة إجراء tRPC**
   - أضف الإجراء في `server/routers.ts`

4. **بناء الواجهة الأمامية**
   - أنشئ صفحة جديدة في `client/src/pages/`
   - استخدم tRPC hooks للاتصال بالخادم

5. **كتابة الاختبارات**
   - أضف اختبارات في `server/*.test.ts`
   - شغّل `pnpm test` للتحقق

### الاختبارات

```bash
# تشغيل جميع الاختبارات
pnpm test

# تشغيل اختبار محدد
pnpm test lessons.test.ts

# الاختبارات المتاحة:
# - auth.logout.test.ts: اختبارات المصادقة
# - lessons.test.ts: اختبارات إدارة الخطط والتوليد
```

## الأداء والتحسينات

### التحسينات المنفذة

1. **تحسين الاستعلامات**
   - استخدام Drizzle ORM للاستعلامات الفعالة
   - فهرسة الحقول المهمة في قاعدة البيانات

2. **تحسين الواجهة**
   - استخدام React Query للتخزين المؤقت
   - تحميل كسول للمكونات الثقيلة
   - تحسين الرسوم المتحركة والانتقالات

3. **تحسين التوليد**
   - استخدام Gemini Flash للسرعة
   - معالجة غير متزامنة للعمليات الطويلة

## استكشاف الأخطاء

### مشاكل شائعة

**المشكلة**: التطبيق بطيء
- **الحل**: تحقق من سرعة الإنترنت، استخدم Gemini Flash بدلاً من Pro

**المشكلة**: فشل التوليد
- **الحل**: تحقق من صحة مفتاح Gemini API، تأكد من الاتصال بالإنترنت

**المشكلة**: لا تظهر الخطط المحفوظة
- **الحل**: تحقق من تسجيل الدخول، امسح ذاكرة التخزين المؤقت

## الدعم والمساهمة

للإبلاغ عن مشاكل أو اقتراح ميزات جديدة، يرجى التواصل عبر:
- البريد الإلكتروني: support@smart-lesson-planner.com
- الموقع: https://smart-lesson-planner.com

## الترخيص

هذا المشروع مرخص تحت MIT License.

---

**آخر تحديث**: يوليو 2026
**الإصدار**: 1.0.0
